{
  "mcpServers": {
    "hrp": {
      "command": "./scripts/hrp-mcp",
      "cwd": ".",
      "env_vars": ["HRP_CLI", "HRP_URL", "HRP_DATA_DIR", "HRP_PORT", "HOME", "PATH"],
      "startup_timeout_sec": 10,
      "tool_timeout_sec": 3600
    }
  }
}
