interface:
  display_name: "Use HRP"
  short_description: "Publica y ejecuta mapas de cambios en HRP"
  default_prompt: "Usa $use-hrp para ejecutar esta tarea con un mapa granular y aprobación humana."
policy:
  allow_implicit_invocation: true
