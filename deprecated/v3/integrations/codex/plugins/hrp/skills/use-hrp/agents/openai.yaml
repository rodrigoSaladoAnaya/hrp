interface:
  display_name: "Use HRP"
  short_description: "Mapas, cambios y revisión multi-modelo en HRP"
  default_prompt: "Usa $use-hrp para ejecutar esta tarea con mapa granular, aprobación humana y revisión multi-modelo."
policy:
  allow_implicit_invocation: true
